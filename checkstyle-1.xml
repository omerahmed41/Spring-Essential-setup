<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
        "-//Puppy Crawl//DTD Check Configuration 1.3//EN"
        "http://www.puppycrawl.com/dtds/configuration_1_3.dtd">

<module name="Checker">

    <module name="TreeWalker">
        <!-- Whitespace checks -->
        <module name="SeparatorWrap"/>
        <module name="WhitespaceAround"/>

        <module name="SeparatorWrap"/>
        <!-- Ensure there are no unused imports -->
        <module name="UnusedImports"/>

        <!-- Disallow line breaks after an assignment operator -->
        <module name="OperatorWrap"/>

        <!-- Style checks for imports -->
        <module name="RedundantImport"/>

        <!-- Require explicit scoping of variables -->
        <module name="VisibilityModifier">
            <property name="packageAllowed" value="false"/>
        </module>

        <!-- Allow single line if statements -->
        <module name="SingleLineJavadoc"/>

        <!-- Require curly braces around if statements, loops, etc. -->
        <module name="NeedBraces"/>

        <!-- Disallow empty block statements -->
        <module name="EmptyBlock"/>

        <!-- Ensure that there is a space before and after operators -->
        <module name="WhitespaceAround"/>

        <!-- Ensure class and method Javadocs are present -->
        <module name="JavadocType"/>
        <module name="JavadocMethod"/>

        <!-- Indentation -->
        <module name="Indentation">
            <property name="basicOffset" value="4"/>
            <property name="braceAdjustment" value="0"/>
            <property name="caseIndent" value="4"/>
            <property name="throwsIndent" value="4"/>
            <property name="arrayInitIndent" value="4"/>
        </module>

        <!-- Naming conventions -->
        <module name="MethodName">
            <property name="format" value="^[a-z][a-zA-Z0-9]*$"/>
        </module>
        <module name="ParameterName">
            <property name="format" value="^[a-z][a-zA-Z0-9]*$"/>
        </module>
        <module name="PackageName">
            <property name="format" value="^[a-z]+(\.[a-z][a-z0-9]*)*$"/>
        </module>
        <module name="NoWhitespaceAfter"/>
    </module>

    <module name="FileTabCharacter"/>

    <!-- Maximum line length -->
    <module name="LineLength">
        <property name="max" value="120"/>
        <property name="ignorePattern" value="^import .*"/>
    </module>



</module>